Email delivery implemented with Resend API.

For test deployment the project uses Resend sandbox sender
(onboarding@resend.dev).

Production deployment can be extended with a verified custom domain
for SPF/DKIM email delivery.